import React from 'react';
import { createRoot } from 'react-dom/client';
import WishExperience from './WishExperience';
createRoot(document.getElementById('root')!).render(<React.StrictMode><WishExperience/></React.StrictMode>);
